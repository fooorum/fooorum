---
import Card from "@src/layouts/Card.astro";

interface Props {
  title: string;
  id: number;
  description: string | null;
}

const { title, id, description } = Astro.props;
---

<Card title={title} url={`/users/${id}`}>
  {description}
</Card>
