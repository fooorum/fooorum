---
interface Props {
    href?: string | null;
}

const { href } = Astro.props;
---

{
    href ? (
        <a href={href}>
            <slot />
        </a>
    ) : (
        <span>
            <slot />
        </span>
    )
}
