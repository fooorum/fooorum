---
import { Icon } from "astro-icon/components";
interface Props {
  postId: number;
}
const { postId } = Astro.props;
---

<button formaction={`/comments/create/${postId}`}>
  <Icon name="material-symbols:reply" />
</button>

<style lang="scss">
  button {
    position: relative;
    z-index: 1;
  }
</style>
