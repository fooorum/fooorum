---
interface Props {
  id?: number;
}
const { id } = Astro.props;
---

<hr transition:name={id === undefined ? undefined : `separator-${id}`} />

<style lang="scss">
  hr {
    color: var(--accent);
    width: 100%;
  }
</style>
