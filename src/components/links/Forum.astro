---
import { Icon } from "astro-icon/components";
interface Props {
  id: number;
}
const { id } = Astro.props;
---

<a class="forum-link" href={`/forums/${id}`}>
  <Icon name="mdi:forum" />
  <slot />
</a>
