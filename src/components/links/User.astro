---
import { Icon } from "astro-icon/components";
interface Props {
  id: number;
}
const { id } = Astro.props;
---

<a rel="author" href={`/users/${id}`}>
  <Icon name="mdi:user" />
  <slot />
</a>

<style>
  a {
    position: relative;
    z-index: 1;
  }
</style>
