---
import Card from "./Base.astro";

interface Props {
    title: string;
    id: number;
    description: string | null;
}

const { title, id, description } = Astro.props;
---

<Card title={title} url={`/forums/${id}`}>
    {description}
</Card>
