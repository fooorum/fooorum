---
import Layout from "@layouts/Layout.astro";
import Card from "@components/cards/User.astro";
import { type UserData } from "@lib/db";
import Separator from "@components/feeds/Separator.astro";

type Props = UserData;
const { name, description } = Astro.props;

let layoutTitle = `${name}`;
if (description) layoutTitle += `: ${description}`;
layoutTitle += ` • Nutzer`;
---

<Layout title={layoutTitle}>
  <Card active {...Astro.props} />
  <Separator />
  <slot />
</Layout>
